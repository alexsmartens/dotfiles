" Fuzzy search for files

Plug 'junegunn/fzf', { 'do': { -> fzf#install() } }
Plug 'junegunn/fzf.vim'

" Customize default GFiles command (fzf#vim#gitfiles)
" `-i` (or `fzf -i`) forces case-insensitive search. Default is smart-case
" match where the search will be case-sensitive if you use at least one
" uppercase letter your pattern and case-insensitive otherwise.
command! -bang -nargs=? -complete=dir GFiles
    \ call fzf#vim#gitfiles(<q-args>, fzf#vim#with_preview({'options': ['-i']}), <bang>0)

" Gfiles searches files only among git files (git ls-files)
" flags to include uncommitted files:
" --others: shows untracked files
" --exclude-standard: makes sure .gitignore is respected
" --cached: shows cached files
noremap <c-p> :GFiles --others --exclude-standard --cached<cr>

" Search among all files
" 'π' is a character generated by <ALT+p> on Mac
noremap π :Files<cr>

" Search text matches in file content (with ripgrep)
noremap <c-f> :Rg<cr>

" vim:ft=vi

